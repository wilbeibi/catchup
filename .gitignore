# Compiled binary from `go build` at the repo root
/catchup

# Test + coverage artifacts
*.test
*.out

# Go workspace files (local-only)
go.work
go.work.sum

# Design docs (local reference only)
/docs

# agent instruction files stay local
CLAUDE.md
AGENTS.md
.claude/
