# Compiled binary from `go build` at the repo root
/catchup

# Test + coverage artifacts
*.test
*.out

# Go workspace files (local-only)
go.work
go.work.sum

# Design docs (local reference only), except shipped release notes
/docs/*
!/docs/releases/

# harness-specific instruction files stay local; AGENTS.md is shared
CLAUDE.md
.claude/
.wrangler/
